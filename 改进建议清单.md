# EnvLab 站点改进建议清单

审查时间：2026-09-12　范围：98 个 HTML 页面（2.7 MB）

---

## 一、功能缺陷（建议优先修）

### 1. 有一页依赖外部 CDN，与「离线可用」的核心承诺冲突　🔴 高

`02-检测实验/水质四项-COD氨氮总磷总氮实验.html` 第 7 行：

```html
<script src="https://cdn.jsdelivr.net/npm/echarts@5.5.0/dist/echarts.min.js"></script>
```

这是**全站唯一的外部依赖**。但 README 明确写着两处承诺：

- 第 3 行：「打开即用，不装插件、不注册、**不需要服务器**」
- 第 61 行：「可直接下载整目录**离线使用（适用于无外网的机房）**」

在无外网机房打开这一页，ECharts 加载失败 → `echarts.init()` 抛错 → 汇总柱状图空白。而且该图表是**写死的静态数据**（42.7 / 0.86 / 0.18 / 1.52），只是为了画 4 根柱子就引入了 1 MB 的图表库。

**建议**：改用纯 CSS 柱状图（4 根柱子 + 3 条限值虚线），约 30 行 CSS 即可，彻底去掉这个依赖。这是「性价比最高的一处修复」——既兑现了离线承诺，又减掉一个网络请求。

### 2. 报告用 `alert()` 弹出，移动端体验差　🟠 中

14 个页面用 `alert(text)` 显示检测报告。以 COD 页为例，`text` 是包含质控数据、结论、签名栏的**多行完整报告**：

```
质控数据：
  · 平行样相对偏差：3.2% ✅
  ...
结论：依据GB 3838-2002，该水样COD指标超出Ⅲ类标准（≤20mg/L），为劣Ⅴ类水质。
检测人：______    审核人：______
```

`alert` 无法选中、无法复制、无法打印，手机上长文本会被截断且换行错乱。

**建议**：改为页内「报告卡」面板展开显示，配一个「复制」按钮（`navigator.clipboard`）。全站目前**剪贴板复制能力为 0 页** —— 学员做完实验想留存结果，只能截图或手抄。

### 3. 94 页缺 `description`，SEO 只做了一半　🟠 中

上一轮我给 4 个入口页加了完整 SEO 标签，但**内容页 94 个全部缺 `meta description`**。这些页面已有 `og:` / `twitter:` 信息，补 description 可以直接复用同一套文案。

同时 `sitemap.xml` 里的 98 条 URL，Google/必应抓取后因无 description，展示的摘要会由搜索引擎自行截取正文首段——对「大气污染控制」「旋风除尘器」这类页面，很可能截出不通顺的片段。

### 4. 两个速查表缺 `viewport`，手机上看是缩小版　🟠 中

- `07-工具速查/气相色谱方法开发速查表.html`
- `07-工具速查/水质评价标准速查表.html`

这两个页面是 A4 打印导向（`@page{size:A4}`），但缺 `viewport` 导致手机上按 980px 虚拟宽度渲染，字体缩到 9pt 以下几乎不可读。其余 96 页都有。

**建议**：补 `<meta name="viewport" content="width=device-width,initial-scale=1">`。如果设计上就是要打印，可以再补一句屏幕端提示「本页为 A4 速查表，建议打印或横屏查看」。

---

## 二、交互与信息架构

### 5. 79 个页面没有任何交叉链接，是「信息孤岛」　🟠 中

统计站内 `.html` 链接密度：**79 个页面的站内链接 ≤ 1 个**（那 1 个还是我刚加的返回按钮）。

这意味着：学员在「COD 检测实验」做完后，无法跳到相关的「水质四项」「水质评价标准速查表」「环境质量综合体检中心」。而这些页面内容上高度关联——COD 检测的结论要用水质标准评价，评价结果又要对照限值表。

**建议**：在每个页面底部（标准依据块上方）加「相关实验」区块，1–3 个链接。例如：

- COD 检测 → 水质四项、氨氮总磷总氮详细版、水质评价标准速查表
- 旋风除尘器 → 袋式除尘器、电除尘器、粉尘分散度

不必全站 79 页都做，**先做 10 个大气污染控制页 + 12 个检测实验页**（内容关联最紧的两组）就能显著改善。

### 6. 全站只有目录页有搜索，站内搜索缺失　🟡 低

98 页里只有 1 个页面（目录页）有搜索框。学员若记得「有个讲 ICP-MS 的页」但不确定在哪个分类下，只能先回目录再搜。

考虑到已有目录页搜索可用，这条优先级不高。如果做，一个轻量方案是：在返回按钮旁再加一个「搜索」图标，点开是一个浮层，内置全部 83 条标题+关键词的 JSON（约 20 KB），纯前端做即时过滤。

---

## 三、工程质量

### 7. 单页 3 个 `<style>` 块，样式分散　🟡 低

45 个页面有 3 个独立 `<style>` 块（原始样式 + 标准依据块样式 + 返回按钮样式）。因为都是我按「追加不改动」的原则注入的，所以形成了这种结构。

**影响**：不影响渲染（CSS 后写覆盖先写），但后续维护时找一个样式要在 3 个块里翻。

**建议**：如果后续还要批量改样式，可以做一次合并（把三个块合成一个，放在 `</head>` 前）。纯粹是整洁性问题，不影响功能。

### 8. 页面体积差异较大　🟡 低

最大 73.8 KB（环境质量综合体检中心），平均 28.6 KB，总计 2.7 MB。对静态教学站完全可接受，无需优化。**仅作记录**。

---

## 四、已验证没有问题的项（供放心）

做了一轮系统性排查，以下均正常，无需处理：

| 检查项 | 结果 |
|---|---|
| 缺 `<title>` | 0 页 |
| 缺 `DOCTYPE` / `charset` / `lang` | 0 页 |
| 重复 `id` 属性 | 0 页 |
| 空文本按钮（无 aria） | 0 页 |
| 站内断链 | 0 / 237 条引用 |
| `sitemap.xml` 与实际文件一致性 | 98 / 98 完全吻合 |
| 页内自述计数与 83 冲突 | 0 页 |
| 孤儿页面（目录页无法到达） | 0 个 |
| 培训系统后端依赖 | 设计良好，无后端时自动回退 localStorage |

特别说明培训系统：`config.js` 里 `window.API_BASE` 为空时自动降级为 localStorage 单机模式，是同源部署与离线使用都能跑的合理设计，**这块做得很好**。

---

## 建议的执行顺序

| 优先级 | 项目 | 预计改动量 |
|---|---|---|
| 1 | 去掉 ECharts CDN，改纯 CSS 柱状图 | 1 页 |
| 2 | `alert()` 改报告面板 + 复制按钮 | 14 页 |
| 3 | 94 页补 `description` | 批量脚本 |
| 4 | 2 页补 `viewport` | 2 页 |
| 5 | 加「相关实验」交叉链接 | 22 页 |

前 4 项都是确定性改动，可以一次做完。第 5 项需要逐页判断关联关系，建议单独一轮。
